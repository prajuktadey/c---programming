#include <iostream>

int main()
{
    char s[]= {'S', 't', 'r', 'i', 'n', 'g', 0}; //null terminated array of characters
    //0 is the null character
    printf("s is %s\n", s);

    return 0;
}

//output

//s is String